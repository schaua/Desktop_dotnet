<UserControl x:Class="Day1.Views.TheView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Day1.Views" xmlns:viewmodels="clr-namespace:Day1.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:TheViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.InputBindings>
            <KeyBinding Key="Enter" Command="{Binding ConvertCommand}"/>
        </Grid.InputBindings>
        <StackPanel Height="336">
            <Label Foreground="Blue" Margin="5,5,5,0">
                Number to double:</Label>
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Label Foreground="Blue" Margin="5,5,5,0">History</Label>
            <ListBox ItemsSource="{Binding History}" Height="200" Margin="5"/>
            <Button Command="{Binding ConvertCommand}" Margin="5">Convert</Button>
        </StackPanel> 
    </Grid>
</UserControl>
